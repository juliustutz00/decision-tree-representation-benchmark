# depencies between the packages are tricky, please refer replace the upper part with the bottom one, currently commented out, to ensure that everything works
# the code was tested to work on Python 3.10.19 and Python 3.12.9 on a Linux system
kmapper
learn2learn
matplotlib
networkx
openTSNE
pandas
POT
pytorch-lightning
PyYAML
scikit-learn
scikit-learn-extra
scipy
seaborn
torch
zss
numpy<2

# The following package versions are ensured to work when using Python 3.10 or Python 3.12 on Kali Linux (Python 3.11 does NOT work)
# on a VM, openTSNE might not work; if so (e.g., it shows a zsh error), do python -m pip uninstall openTSNE -y && pip install --no-binary :all: openTSNE
#kmapper==1.2.0
#learn2learn==0.2.0
#matplotlib==3.8.4
#networkx==3.4.2
#openTSNE==1.0.4
#pandas==2.3.3
#POT==0.9.6
#pytorch-lightning==2.6.1
#PyYAML==6.0.3
#scikit-learn==1.7.2
#scikit-learn-extra==0.3.0
#scipy==1.15.3
#seaborn==0.13.2
#torch==2.10.0
#zss==1.2.0
#numpy==1.26.4
